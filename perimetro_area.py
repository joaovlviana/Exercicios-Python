#Recebe o lado de um quadrado inserido pelo usuário e retorna área e perímetro

lado = float(input("Insira o lado do quadrado:"))
perimetro = lado*4
area = lado**2

print(f"perímetro {perimetro}, área {area}")

